# Tensor.max()

Returns the maximum value of a tensor.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([1, 2, 3, 4])

print(a1.max().numpy())
```

## Return value
```bash
4.0
```