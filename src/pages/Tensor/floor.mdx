# Tensor.floor()

Returns the largest integer less than or equal to each element of the input tensor.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([[1.23423, 2, 3, 4]])
a1 = a1.floor()

print(a1.numpy())
```

## Return value
```bash
[[1. 2. 3. 4.]]
```