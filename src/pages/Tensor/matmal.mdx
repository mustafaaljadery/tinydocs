# Tensor.matmul()

Returns the matrix product of two tensors.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([1, 2, 3, 4])
a2 = Tensor([2, 3, 4, 5])
a2 = a2.reshape(4, 1)

a1 = a1.matmul(a2)

print(a1.numpy())
```

## Return value
```bash
[40.]
```