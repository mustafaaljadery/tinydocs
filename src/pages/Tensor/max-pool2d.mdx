# Tensor.max_pool2d()

Returns the maximum value of a 2D tensor.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([[1, 2], [3, 4]])
a1 = a1.max_pool2d()


print(a1.numpy())
```

## Return value
```bash
[[4.]]
```