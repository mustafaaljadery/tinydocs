# Tensor.numel()

Returns the number of elements in a tensor.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([1, 2, 3, 4])
a1 = a1.numel()

print(a1)
```

## Return value
```bash
4
```