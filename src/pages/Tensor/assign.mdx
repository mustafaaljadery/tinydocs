# Tensor.assign()

Assigns tensors, and ensures the tensors match in shape and device.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([1, 2, 3, 4])
a2 = Tensor([5, 6, 7, 8])
a1 = a1.assign(a2)

print(a1.numpy())
```

## Return value
```bash
[5. 6. 7. 8.]
```