# Tensor.min()

Returns the minimum value of a tensor.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([1, 2, 3, 4])

print(a1.min().numpy())
```

## Return value
```bash
1.0
```