# Tensor.div()

Returns the division of two tensors.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([1, 2, 3])
a2 = Tensor([1, 2, 3])

a1 = a1.div(a2)

print(a1.numpy())
```

## Return value
```bash
[1. 1. 1.]
```