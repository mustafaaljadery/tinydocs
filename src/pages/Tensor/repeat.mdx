# Tensor.repeat()

Returns the tensor repeated along the specified dimensions.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([[1, 2, 3], [3, 4, 5]])
a1 = a1.repeat([1, 2])

print(a1.numpy())
```

## Return value
```bash
[[1. 2. 3. 1. 2. 3.]
 [3. 4. 5. 3. 4. 5.]]
```