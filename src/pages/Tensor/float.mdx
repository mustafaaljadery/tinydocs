# Tensor.float()

Changes the data type of the tensor to float32.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([[1, 2], [3, 4]])
a1 = a1.float()


print(a1.numpy())
```

## Return value
```bash
[[1. 2.]
 [3. 4.]]
```