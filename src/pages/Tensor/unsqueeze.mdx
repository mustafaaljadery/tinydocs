# Tensor.unsqueeze()

Returns the tensor with a dimension of size one inserted at the specified position.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([1, 2, 3])
a1 = a1.unsqueeze(1)

print(a1.numpy())
```

## Return value
```bash
[[1.]
 [2.]
 [3.]]
```