# Tensor.element_size()

Returns the size of the elements in the tensor.

## Usage
```python copy
from tinygrad.tensor import Tensor

a1 = Tensor([1, 2, 3, 4, 5])
a1 = a1.element_size()

print(a1)
```

## Return value
```bash
4
```